name: Continuous Integration and Deployment

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Allow only one concurrent deployment
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Required for paths-filter and Codecov
permissions:
  contents: read
  pull-requests: read

jobs:
  # Detect which parts of the codebase changed
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      deploy: ${{ steps.filter.outputs.deploy }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check for file changes
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            backend:
              - 'app/**'
              - 'scripts/**'
              - 'tests/**'
              - 'README.md'
              - 'fly.acceptance.toml'
              - 'pyproject.toml'
              - 'uv.lock'
              - 'docker/Dockerfile'
              - 'docker/Dockerfile.base'
              - 'fly.toml'
              - 'docker/docker-compose.yml'
              - 'docker/supervisord.conf'
              - 'docker/entrypoint.sh'
              - 'docker/config/qdrant.yaml'
              - '.github/workflows/ci-cd.yml'
            deploy:
              - 'app/**'
              - 'scripts/**'
              - 'pyproject.toml'
              - 'uv.lock'
              - 'docker/Dockerfile'
              - 'docker/Dockerfile.base'
              - 'fly.toml'
              - 'fly.acceptance.toml'
              - 'docker/supervisord.conf'
              - 'docker/entrypoint.sh'
              - 'docker/config/qdrant.yaml'

  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.backend == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Install dependencies
        run: uv sync --extra test

      - name: Run backend tests with coverage
        run: uv run pytest tests/ -v --tb=short --cov=app --cov=scripts --cov-report=xml --cov-report=html

      - name: Upload coverage reports
        uses: codecov/codecov-action@v7
        with:
          files: ./coverage.xml
          flags: backend
          name: backend-coverage
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'

  lint-python:
    name: Lint Python code
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    steps:
      - id: checkout
        name: Checkout
        uses: actions/checkout@v6

      - id: check-ruff
        name: Check with Ruff
        uses: astral-sh/ruff-action@v3
        with:
          args: "check --exit-zero --verbose"

      - id: format-ruff
        name: Format with Ruff
        uses: astral-sh/ruff-action@v3
        with:
          args: "format --check --verbose"

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Install dependencies
        run: uv sync --extra test --extra mypy --extra dev

      - name: Run ty
        run: uv run ty check app/publication_contract.py app/publication_routes.py app/publication_service.py

      - name: Run mypy
        run: uv run mypy .

  # --------------------------------------------------------------------------
  # NOTE: This job auto-deploys to IRE's staging Fly.io instance only. It never
  # touches production. Promoting a specific staged commit to production is a
  # deliberate, separate action — see .github/workflows/promote-production.yml.
  # Fork maintainers: update the app name below and the verification URL,
  # then add FLY_API_TOKEN to your repo's Settings → Secrets → Actions.
  # --------------------------------------------------------------------------
  deploy-staging:
    name: Deploy backend to staging (Fly.io)
    runs-on: ubuntu-latest
    needs: [changes, backend-tests, lint-python, type-check]
    if: |
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.changes.outputs.deploy == 'true' &&
      !contains(github.event.head_commit.message, '[skip deploy]')
    environment:
      name: staging-backend
      url: https://ire-archive-acceptance-search.fly.dev
    concurrency:
      group: staging-backend-ops
      cancel-in-progress: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Fly.io CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy to Fly.io staging
        run: flyctl deploy --remote-only --config fly.acceptance.toml --app ire-archive-acceptance-search
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Verify deployment
        run: |
          echo "Waiting 30s for deployment to stabilize..."
          sleep 30
          curl -f https://ire-archive-acceptance-search.fly.dev/healthz || exit 1
          echo "✅ Staging deployment verified!"
          echo "To promote this exact commit to production, run the 'Promote staged backend to production' workflow with this run's ID: ${{ github.run_id }}"
